export default function () {
  return {
    allComponents: [], // array used for listing in selection dialog
    isDark: false,
    defaultDays: 30, // for time series, default days to go back
  };
}
